# Acceptance criteria — one executable demonstration per line, run from the
# project root. All must exit 0 and produce real output. Everything below runs
# the real product offline via the deterministic rule-based backend.

# 1. Full five-pass pipeline + style profile on a 142-word wordy draft.
python3 acceptance.py

# 2. The CLI entry point, end to end, reading a draft from stdin.
echo "In the event that you utilize this, it is of the utmost importance to commence promptly. Don't delay -- each and every very small step helps!" | python3 -m writing_assistant --backend rules

# 3. The test suite is itself reproducible from the project root.
python3 -m pytest tests/ -q
